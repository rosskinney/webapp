<h1>All Portfolio Items</h1>

<ul>
  <% @projects.each do |project| %>
    <li>
      <%= link_to project.title, project_path(project) %>
    </li>
  <% end %>
</ul>
<% if logged_in? %>
<%= link_to "New Portfolio Project", new_project_path, :class => "new_project" %>
 <% end %>