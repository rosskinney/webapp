<h1>All Categories</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
    <%= link_to tag.name, tag_path(tag) %>
    &nbsp
    <%= link_to "destroy", tag_path(tag), :method => :delete %>
    </li>
  <% end %>
</ul>